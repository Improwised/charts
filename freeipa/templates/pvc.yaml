apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freeipa-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: {{ .Values.volumestorage }}
