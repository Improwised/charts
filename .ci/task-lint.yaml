---
platform: linux
image_resource: { type: docker-image, source: { repository: quay.io/helmpack/chart-testingm} }

inputs:
- name: repo

run:
  path: bash
  args:
  - -cx
  - |
    cd repo
    export HELM_CONFIG_HOME=./
    ct lint --chart-dirs charts --debug
